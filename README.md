# Instagram pnpm monorepo micro front

## Project Structure
- **Shell**: The main container app that integrates remote components (like `auth`).
- **Auth**: A remote app that handles authentication (e.g., login, signup).

```
pnpm install
npm run build
npm run serve
```